<h1>PlantTags#new</h1>
<%= simple_form_for [@plant, @plant_tag] do |f| %>
  <%= f.input :tag_id, collection: Tag.all, input_html: {multiple: true, data: {controller: "select2"}}, include_hidden: false %>
  <%= f.submit "Add tag", class: 'btn btn-primary' %>
<% end %>
